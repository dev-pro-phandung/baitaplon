name: Deploy Minimart To Ubuntu

on:
  push:
    branches:
      - main 

jobs:
  build-and-deploy:
    runs-on: self-hosted 

    steps:
      - name: Lay ma nguon moi nhat
        uses: actions/checkout@v4

      - name: Dong goi va chay Docker
        run: |
          sudo docker compose down
          sudo docker compose up -d --build
